require 'rails_helper'

RSpec.describe MealFood, type: :model do
  context "of calorie_consumption" do
    it "automatically calculated when it's created" do
    end
    it "automatically calculated when it's updated" do
    end
  end
end
